# Git hooks managed by lefthook (https://lefthook.dev)
# Installed automatically via the root `prepare` script after `bun install`.

pre-push:
  parallel: true
  commands:
    test:
      run: bun test --run
